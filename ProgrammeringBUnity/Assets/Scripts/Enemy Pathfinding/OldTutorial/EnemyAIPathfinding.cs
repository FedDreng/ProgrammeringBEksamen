using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemyAIPathfinding 
{
    //private Grid<>
    private Grid grid;

    public EnemyAIPathfinding(int width, int height)
    {
        //grid = new Grid(width, height, 10f, Vector3.zero, (Grid grid, int x, int y) => new PathNode(g, x, y));
    }

}
